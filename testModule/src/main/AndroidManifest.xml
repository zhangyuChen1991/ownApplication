<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sz.china.testmoudule">

    <permission android:name="android.permission.INTERNET"></permission>
    <application
        android:name=".application.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ImmersionActivity"
            android:theme="@android:style/Theme.Translucent"></activity>

        <activity
            android:name=".TestRotateAnimationActivity"
            android:theme="@android:style/Theme.Translucent"></activity>
        <activity android:name=".TestScanAcy"></activity>
        <activity android:name=".TestActivity1"></activity>
        <activity android:name=".TestUnlockViewActivity"></activity>
        <activity android:name=".RecycleViewDemoActivity"></activity>
        <activity android:name=".PullToRefreshScrollViewActivity"></activity>
        <activity android:name=".SwipeRefreshLayoutActivity"></activity>
    </application>

</manifest>